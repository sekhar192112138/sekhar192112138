#include <stdio.h>

int main()
{
    int i, j,a;
    printf("enter the number of lines= ");
    scanf("%d",&a);
    for(i=1;i<=a;i++)
    {
        for(j=1;j<=i;j++)
        {
            printf("0.%d ",i);
        }
        printf("\n");
    }

    return 0;
}
